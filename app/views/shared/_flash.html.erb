<% flash.each do |key, value| %>
  <div class="alert alert-<%= (key == 'notice') ? 'warning' : 'success' %> fade in">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
    <p><%= value %></p>
  </div>
<% end %>
